version: '3.8'

services:
  web:
    build: .
    ports:
      - "5000:5000"
    volumes:
      - ./uploads:/app/uploads
      - ./instance:/app/instance
    environment:
      - FLASK_ENV=development
      - DATABASE_URL=sqlite:///instance/eln.db
    restart: unless-stopped
